{
  "identifier": "default",
  "description": "Default capabilities for Resonance",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:allow-spawn",
    "shell:allow-execute"
  ]
}
